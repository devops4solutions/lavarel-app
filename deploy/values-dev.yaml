# Default values for phpfpm-app.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  registry: gcr.io
  repository: www-hosting-dev-6172682f/innovate-inclusion
  tag: latest
  pullPolicy: IfNotPresent
nginxService:
  name: nginx
  type: ClusterIP
  externalPort: 443
  internalPort: 8443
  docRoot: /app/public
phpfpmService:
  name: phpfpm
  type: ClusterIP
  phpfpmPort: 9000
  indexFile: index.php
persistence:
  enabled: false
  storageClass: "nfs"
  accessMode: ReadWriteMany
  size: 3Gi
mariadb:
  enabled: true
  replication:
    enabled: false
  db:
    name: innovate_inclusion_dev
    user: innovateinclusion
    password: innovateinclusion
  master:
    persistence:
      enabled: false
      # storageClass: "-"
      accessModes:
        - ReadWriteOnce
      size: 2Gi
ingress:
  enabled: true
  hosts:
    - "innovate-inclusion.dev.k8s.kpd-i.com"
  annotations:
    kubernetes.io/ingress.class: "nginx"
    kubernetes.io/tls-acme: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
  tls:
  - hosts:
    - innovate-inclusion.dev.k8s.kpd-i.com
    secretName: innovate-inclusion-www-dev-tls
resources:
  limits:
    cpu: 120m
    memory: 256Mi
  requests:
    cpu: 80m
    memory: 128Mi
